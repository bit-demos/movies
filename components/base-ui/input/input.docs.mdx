---
labels: ['react', 'typescript', 'input', 'text']
description: 'A basic input component.'
---

import {useState} from 'react';
import {Input} from './input';

A basic input component, with themeable styling which can be overriden.



Basic example:
Using the input component with a placeholder and style:

```js live
() => {
  const [value, setValue] = useState('');
  return <Input placeHolder="Search" style={{color: 'blue'}} onChange={e => setValue(e.currentTarget.value)} value={value} />;
};
```
