---
description: A context components for managing movies favourites data across an application
labels: ['favourites', 'context', 'provider', 'react']
---

A context component for managing movies favourites context for an application. 

This is a specific implementation of the [generic Favourites context component](https://bit.dev/learn-harmony/movies/context/favourites-context), with 
data type specific as the Movie model. 

To be used in our movies application to register favourite movies.

Example usage:
```
import {} 

```