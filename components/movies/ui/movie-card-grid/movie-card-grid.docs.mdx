---
labels: ['react native', 'typescript', 'movie', 'card', 'list']
description: 'A movie card list component.'
---
import { Movie, mockMovieList } from '@learn-harmony/movies.models.movie';
import { MovieCardGrid} from './index';

The movie-card-grid displays a list of movie-cards in a grid, with their title and the poster of the movie.

See this component's compositions for an example that shows a list of five movies:

